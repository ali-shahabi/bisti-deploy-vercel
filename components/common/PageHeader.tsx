import Head from "next/head";
import React, { ReactNode } from "react";

const PageHeader = (props: ReactNode) => {
  return (
    <Head>
      <title>Mr Whoopee</title>
    </Head>
  );
};

export default PageHeader;
